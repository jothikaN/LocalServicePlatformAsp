@model Services

<div class="container" style="margin-top:150px">
    <div class="row">
        <div class="col-md-12">
            <h2 class="text-uppercase text-center text-decoration-underline">Create Service</h2>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12 border">
            <form method="post" enctype="multipart/form-data">
                @*multipart/form-data-To sore image*@
                <div class="mb-3">
                    <label asp-for="@Model.Name" class="form-label">
                        @* Lable name like brand name ..we can acces through *@
                    </label>
                    <input asp-for="@Model.Name" class="form-control" />
                    @*validation ie showing the error to users*@
                    <span asp-validation-for="@Model.Name" class="text-danger"></span>

                </div>
                <div class="mb-3">
                    <label asp-for="@Model.Description" class="form-label">  </label>
                    <input asp-for="@Model.Description" class="form-control" />
                    <span asp-validation-for="@Model.Description" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <label asp-for="@Model.ServicePic" class="form-label">  </label>
                    <input type="file" name="files" id="uploadBox" class="form-control" />

                </div>
                <div class="mb-3">
                    @*go to Back*@
                    <a asp-controller="Services" asp-action="Index" class="btn btn-danger">Back</a>
                    <button type="submit" class="btn-success">Create</button>

                </div>

            </form>
        </div>
    </div>

</div>
@*To avoid hit the server back*@
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}